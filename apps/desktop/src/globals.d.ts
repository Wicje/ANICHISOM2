// React's JSX namespace and global types come from @types/react; with
// `"types": []` set in tsconfig we opt into them explicitly instead of
// letting tsc auto-scan every workspace @types package.
/// <reference types="react" />
/// <reference types="react-dom" />