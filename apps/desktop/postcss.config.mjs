// Continua Desktop uses plain CSS (no Tailwind). This empty config shadows
// the repo-root Next.js/Tailwind `postcss.config.mjs` so Vite doesn't try
// to load `@tailwindcss/postcss` during the desktop frontend build.
export default {};